# Codex TUI Port Inventory

This inventory supports EP-001 of `tasks/prd-codex-tui-parity.md`. It records
which local Codex modules Pyxis may port, how each target is classified, and
which license obligations apply before any code is copied or adapted.

## Classification

| Class | Meaning | Rule |
|---|---|---|
| `copy` | Verbatim or near-verbatim source reuse. | Preserve Apache-2.0 notices before merge. |
| `adapt` | Source is structurally derived from Codex with Pyxis-specific edits. | Preserve Apache-2.0 provenance before merge. |
| `inspired` | Concepts and behavior are studied, but Pyxis code is written independently. | No copied source notice required, keep this inventory current. |
| `skip` | Out of scope, unclear provenance, or blocked until clarification. | Do not port until reclassified. |

## License Decision

Pyxis remains GPL-3.0-or-later. The Apache Software Foundation documents that
Apache-2.0 source can be included in GPLv3 projects, while the reverse direction
is not compatible for Apache projects. The FSF license list also marks
Apache-2.0 as compatible with GPLv3 and not GPLv2.

Operational obligations for this repo:

1. Keep `NOTICE-CODEX.md` when any Codex source is copied or adapted.
2. Preserve upstream Apache-2.0 license and notice text for copied or adapted
   files.
3. Mark any unclear provenance as `skip`.
4. Keep `agent-core` headless: no Ratatui, Crossterm, ANSI, or Codex UI code in
   the core crate.

Sources:

- ASF GPL compatibility: https://www.apache.org/licenses/GPL-compatibility.html
- GNU license list: https://www.gnu.org/licenses/license-list.html#apache2
- ASF license FAQ: https://www.apache.org/foundation/license-faq.html

## Feature Validation

The default rollback path must keep passing through the normal workspace gates.
The parity scaffold is behind `agent-tui/codex_tui_parity`, so EP-001 coverage
also requires:

```powershell
cargo test -p agent-tui --features codex_tui_parity
```

## Current Inventory

| Pyxis target | Codex source | Class | Notes |
|---|---|---|---|
| `crates/agent-tui/src/chatwidget/**` | `/home/arthur/dev/codex/codex-rs/tui/src/chatwidget.rs`, `chatwidget/transcript.rs`, `chatwidget/rendering.rs`, `chatwidget/user_messages.rs` | `adapt` | `ChatWidget` devient le propriétaire unique du mapper, du transcript actif/finalisé et du routage vers le bottom pane. Le runtime, le scrollback terminal et le composer restent des responsabilités séparées. |
| `crates/agent-tui/src/app_event.rs` | `C:\dev\codex\codex-rs\tui\src\app_event.rs`, `chatwidget\command_lifecycle.rs` | `adapt` | Pyxis keeps its own `AgentEvent` adapter, but the lifecycle split for active/finalized transcript items follows Codex. |
| `crates/agent-tui/src/history_cell.rs` | `C:\dev\codex\codex-rs\tui\src\history_cell\mod.rs`, `history_cell\messages.rs`, `history_cell\approvals.rs`, `history_cell\plans.rs`, `history_cell\search.rs`, `history_cell\separators.rs`, `history_cell\mcp.rs`, `history_cell\patches.rs`, `history_cell\request_user_input.rs`, `history_cell\notices.rs`, `history_cell\session.rs`, `history_cell\hook_cell.rs`, `exec_cell\model.rs`, `exec_cell\render.rs` | `adapt` | Pyxis keeps a monolithic file for now, but the `HistoryCell` raw/rich/transcript contract, approval cells, plan updates, web search, MCP calls, request-user-input results, special notices, patch summaries/failures, final separators, session headers, hook runs, exec transcript lines, and output limits are structurally derived from Codex. |
| `crates/agent-tui/src/streaming.rs` | `C:\dev\codex\codex-rs\tui\src\streaming\controller.rs` and `streaming\table_holdback.rs` | `inspired` | Stable-prefix and tail behavior is documented, not ported. |
| `crates/agent-tui/src/bottom_pane.rs` | `C:\dev\codex\codex-rs\tui\src\bottom_pane\mod.rs`, `bottom_pane_view.rs`, `approval_overlay.rs`, `list_selection_view.rs`, `chat_composer.rs` | `inspired` | EP-001 creates the gated module only. Full composer and views stay in later stories. |
| `crates/agent-tui/src/state.rs`, `crates/agent-cli/src/interactive.rs`, `crates/agent-cli/src/settings.rs`, `crates/agent-tools/src/registry.rs` | `C:\dev\codex\codex-rs\tui\src\chatwidget\permissions_menu.rs`, `permission_popups.rs`, `app\config_persistence.rs`, `app\event_dispatch.rs`, `status_surfaces.rs` | `inspired` | Pyxis mirrors the `/permissions` picker, history notice shape, runtime mode update, and user-level persistence, but maps it to Pyxis-owned `PermissionMode` values rather than Codex permission profiles. |
| `crates/agent-tui/src/state.rs`, `crates/agent-cli/src/interactive.rs`, `crates/agent-core/src/agent.rs`, `crates/agent-core/src/provider.rs`, `crates/agent-provider/src/chatgpt.rs` | `C:\dev\codex\codex-rs\tui\src\chatwidget\model_popups.rs`, `C:\dev\codex\codex-rs\protocol\src\openai_models.rs`, `C:\dev\codex\codex-rs\core\src\client.rs`, `C:\dev\codex\codex-rs\models-manager\models.json` | `inspired` | Pyxis keeps `/models` and `/effort` as separate slash commands, but mirrors Codex's reasoning effort values, labels, per-model supported effort catalog, default effort reset, and `ultra` request mapping to `max`. |
| `crates/agent-tui/src/render.rs` | `C:\dev\codex\codex-rs\tui\src\chatwidget\rendering.rs`, `C:\dev\codex\codex-rs\tui\src\render\renderable.rs`, `C:\dev\codex\codex-rs\tui\src\app.rs` | `inspired` | Parity layout borrows Codex's transcript and bottom-pane separation, but Pyxis intentionally keeps the composer anchored at the terminal bottom and renders the transcript tail above it. |
| `crates/agent-tui/src/footer.rs`, `render_input` et `footer_props` de `crates/agent-tui/src/render.rs` | `/home/arthur/dev/codex/codex-rs/tui/src/bottom_pane/footer.rs`, `bottom_pane/chat_composer.rs` (`layout_areas`, `desired_height`, `render_with_mask`, `footer_mode`, `handle_shortcut_overlay_key`), `bottom_pane/status_line_style.rs`, `ui_consts.rs` | `adapt` | Refonte de la status line et du composer (2026-07-31), baseline `/home/arthur/dev/codex` commit `f0c30e528a54bdf0fa9a4d52ff74b34383434811`. Aucune ligne copiée. Sont repris comme CONTRAT : la gouttière de 2 colonnes partagée par le composer et le pied de page ; le placeholder sur composer vide ; la cascade de `FooterMode` (instruction transitoire > rappel de quit > contexte ambiant) ; la status line à gauche en segments ` · ` accentués par catégorie ; l'indicateur de mode aligné à droite avec marge de 2 colonnes ; la règle d'effondrement (l'indicateur tombe avant que la gauche soit tronquée à `…`) ; le pense-bête `?` en deux colonnes, ouvert seulement sur composer vide. Écarts assumés : les modes `EscHint`, `HistorySearch` et `queue` de Codex ne sont pas portés (Pyxis n'a ni backtrack, ni recherche d'historique incrémentale, ni file de soumission) ; le mode de permission remplace l'indicateur de mode collaboratif et ne s'affiche que hors valeur par défaut ; les accents de `status_line_style.rs` (dix catégories thématisées) se réduisent aux deux teintes que la palette Pyxis réserve au sens ; le menu de complétion reste AU-DESSUS du composer là où Codex le rend en dessous, à la place du pied de page ; l'encadrement du composer reste celui de Pyxis (deux filets pleine largeur, pied de page collé au filet de fermeture) au lieu des lignes vides et de la ligne d'espacement de Codex. |
| `crates/agent-tui/src/state.rs`, `crates/agent-tui/src/render.rs`, `crates/agent-tui/src/term.rs`, `crates/agent-cli/src/interactive.rs` | `C:\dev\codex\codex-rs\tui\src\app.rs`, `chatwidget.rs`, `bottom_pane\mod.rs`, `bottom_pane\chat_composer.rs` | `inspired` | Pyxis mirrors Codex shutdown feedback for confirmed quit: the composer shows `Shutting down...`, footer hints are hidden, one final frame is drawn, then the terminal UI is cleared before restore. |
| `crates/agent-tui/src/insert_history.rs` | `C:\dev\codex\codex-rs\tui\src\insert_history.rs`, `terminal_hyperlinks.rs` | `adapt` | Inline insertion remains Pyxis-specific, but finalized history now preserves styled Ratatui lines instead of flattening to plain text. |
| `crates/agent-tui/src/terminal_hyperlinks.rs` | `C:\dev\codex\codex-rs\tui\src\terminal_hyperlinks.rs` | `adapt` | Simplified Pyxis implementation of Codex's separate visible text plus terminal hyperlink metadata model. |
| `crates/agent-tui/src/term.rs` | `C:\dev\codex\codex-rs\tui\src\tui.rs` | `inspired` | The parity path keeps native terminal scrollback available, clears startup build output from the visible screen, and uses a full-height inline viewport for Pyxis's bottom composer preference. |
| `crates/agent-tui/src/terminal_viewport.rs` | `C:\dev\codex\codex-rs\tui\src\tui.rs` and `insert_history.rs` | `inspired` | Viewport geometry scaffold only. |
| `crates/agent-core/src/sandbox.rs` | `codex-rs/protocol/src/protocol.rs:995-1078` (`enum SandboxPolicy`, `struct WritableRoot`, `is_path_writable`), `codex-rs/protocol/src/config_types.rs:86` (`enum SandboxMode`) | `inspired` | EP-001 de `tasks/prd-parite-codex-par-le-code.md`. Le modèle est repris (variantes porteuses de données, racines writables avec sous-chemins soustraits, accès réseau porté par la variante), le code est écrit indépendamment. Écarts assumés : pas de variante `ExternalSandbox` (Pyxis ne délègue jamais le confinement à un runtime englobant), sous-chemins déclarés RELATIFS à leur racine, et `protected_metadata_names` non porté (`.git` est protégé en entier, ce qui le subsume). |
| `crates/agent-runtime/**` (nouveau crate), `crates/agent-session/src/thread_store.rs` | `/home/arthur/dev/codex/codex-rs/core/src/codex_thread.rs`, `core/src/state/{session,turn}.rs`, `core/src/tasks/{lifecycle,regular}.rs` | `inspired` | EP-001 de `tasks/prd-runtime-orchestration-durable.md` (ADR-12). Seuls la SÉPARATION DES RESPONSABILITÉS et les noms de concepts ont été étudiés (propriétaire de thread distinct du moteur de turn, état séparé par durée de vie, un seul turn régulier à la fois). Aucune ligne de source lue ni transposée : le mailbox borné, le `ThreadStore`, les identifiants préfixés, le cycle de vie à six états et le shutdown en quatre temps sont écrits indépendamment à partir des critères d'acceptation du PRD. Écarts assumés : pas d'app-server ni de protocole réseau, événements live en `broadcast` borné, limites en constantes sans clé de configuration. |
| `crates/agent-sandbox/src/proxy.rs` | `codex-rs/protocol/src/protocol.rs:1003-1006,1028-1030` (`network_access`) | `inspired` | EP-001/US-003. L'accès réseau devient une donnée de la politique. La correspondance par suffixe sur frontière d'étiquette et les grants d'un seul appel sont propres à Pyxis : Codex n'a pas de proxy d'allow-list. |
| `crates/agent-tools/src/sandbox.rs`, `crates/agent-tools/src/registry.rs` | `codex-rs/shell-escalation/` | `inspired` | EP-001/US-004. L'idée d'imputer un échec au confinement et de proposer une réexécution élargie est reprise ; l'implémentation diverge par nécessité. Codex confine par commande, Pyxis confine le processus une fois pour toutes (Landlock, hérité et irréversible), donc seul le périmètre réseau s'élargit en place et le périmètre filesystem est nommé sans être escaladable. |
| `crates/agent-cli/src/settings.rs`, `crates/agent-cli/src/main.rs` | `codex-rs/config/src/config_layer_source.rs:6-50` (`enum ConfigLayerSource`, `fn precedence() -> i16`), `codex-rs/config/src/profile_toml.rs:24-48` (`struct ConfigProfile`), `codex-rs/config/src/overrides.rs:7` (`build_cli_overrides_layer`), `codex-rs/protocol/src/protocol.rs:908-928` (`enum AskForApproval`) | `inspired` | EP-002 de `tasks/prd-parite-codex-par-le-code.md`. Le modèle est repris (couches nommées, `precedence()` explicite, profil appliqué comme une couche entre le fichier utilisateur nu et les surcharges de session, surcharges `-c` en couche de plus forte précédence), le code est écrit indépendamment. Les valeurs de précédence suivent Codex là où les couches se correspondent (utilisateur 10/20, profil +5, session 30). Écarts assumés : pas de couche MDM, système, ni enterprise (Pyxis est mono-utilisateur local) ; une couche `environment` en plus, qui existait déjà dans la chaîne implicite de Pyxis ; `-c` refuse les clés de sécurité, ce que Codex ne fait pas ; les surcharges sont des clés plates et non des chemins pointés, parce qu'aucune clé de Pyxis n'est imbriquée. |
| `crates/agent-tools/src/plan.rs`, `crates/agent-core/src/event.rs` | `codex-rs/core/src/tools/handlers/plan_spec.rs:7-58` (`create_update_plan_tool`) | `inspired` | EP-003/US-009. Le modèle de données est repris (items `{ step, status }`, trois états aux mêmes noms de fil, invariant d'au plus un `in_progress`), le code est écrit indépendamment. Écarts assumés : le plan voyage comme variante d'`AgentEvent` plutôt que par le protocole app-server de Codex, et le refus d'un plan invalide est rendu au modèle comme erreur de validation, donc avant toute décision de permission. |
| `crates/agent-tools/src/patch.rs` | `codex-rs/apply-patch/src/parser.rs` (grammaire `apply_patch.lark`), `codex-rs/core/src/tools/handlers/apply_patch.rs` | `inspired` | EP-003/US-010. Seul le FORMAT est repris (marqueurs `*** Begin Patch` / `Add File` / `Delete File` / `Update File` / `Move to` / `@@` / `*** End of File`, préfixes ` `, `+`, `-`) ; l'implémentation est écrite indépendamment, sans reprise des 4 553 lignes du crate amont ni de sa dépendance `starlark`-adjacente. Écarts assumés : l'enveloppe est un paramètre JSON `input` et non un payload d'outil `Custom` (Pyxis n'a pas de canal freeform) ; la recherche de contexte reprend l'échelle de tolérance de l'outil `edit` de Pyxis (exact, `trim_end`, `trim`) au lieu de `seek_sequence` ; l'application est all-or-nothing en mémoire avant la première écriture, ce qui ferme le critère de non-modification partielle. |
| `crates/agent-tools/src/image.rs` | `codex-rs/core/src/tools/handlers/view_image_spec.rs:15-40` (`create_view_image_tool`) | `inspired` | EP-003/US-011. Paramètre `path` unique et intention reprises, code écrit indépendamment. Écarts assumés : le type de média est décidé par les octets magiques du fichier et non par son extension ; le paramètre de niveau de détail n'est pas porté ; l'appel est refusé quand les capabilities du provider ne déclarent pas la vision, ce que Codex ne fait pas. |
| `crates/agent-tools/src/exec_session.rs`, `crates/agent-tools/src/pty.rs` | `codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs`, `write_stdin.rs`, `codex-rs/core/src/tools/handlers/shell_spec.rs:21-155,264-296` (paramètres et `unified_exec_output_schema`), `codex-rs/core/src/tools/context.rs:444-470` (`response_text`), `codex-rs/core/src/unified_exec/mod.rs:64-72` (bornes de yield, marqueur d'omission), `process_manager.rs:76-88` (`UNIFIED_EXEC_ENV`) | `inspired` | EP-003/US-012 puis EP-004/US-014 et US-015 de `tasks/prd-parite-totale-codex-cli.md`. La séparation en deux outils, les NOMS du wire (`cmd`, `workdir`, `shell`, `tty`, `yield_time_ms`, `max_output_tokens`, `chars`), la forme du résultat (`chunk_id`, `wall_time_seconds`, `exit_code`, `session_id`, `original_token_count`, `output`), le texte rendu au modèle, les bornes de yield (250-30000 ms, défauts 10000/250/5000), l'approximation de 4 octets par token, le marqueur `... N bytes omitted ...` et l'environnement neutralisé (`TERM=dumb`, `NO_COLOR`, pagers) sont repris parce que ce sont les identifiants du contrat que les modèles ont appris. Le code est écrit indépendamment ; le PTY est ouvert avec `openpty`/`setsid`/`TIOCSCTTY` sans reprendre `portable-pty`. Écarts assumés : `chunk_id` est un compteur MONOTONE par session (le PRD l'exige) là où Codex tire 6 chiffres hexadécimaux aléatoires ; une session à tubes accepte l'entrée standard, que Codex réserve au PTY ; `write_stdin` porte un paramètre `terminate` (Codex ne termine que par le protocole app-server ou le caractère d'interruption) ; un yield explicite est honoré dès 250 ms même sur un sondage vide, le plancher de 5000 ms restant le DÉFAUT ; plafond de quatre sessions, fermeture sur inactivité, garde-fou de sous-chemins protégés sur l'entrée standard et refus avant spawn (cwd, shell, cinquième session) propres à Pyxis. |
| `crates/agent-mcp/src/http.rs`, `crates/agent-mcp/src/client.rs` | `codex-rs/rmcp-client/src/http_client_adapter.rs:38-85` (`impl StreamableHttpClient`) | `inspired` | EP-004/US-013. La forme est reprise : implémenter le trait `StreamableHttpClient` de `rmcp` sur un client HTTP maison plutôt qu'activer la feature `-reqwest` de `rmcp`, pour la même raison que Codex (le SDK épingle reqwest 0.13, le workspace est sur 0.12 : l'activer dupliquerait toute la pile HTTP/TLS). Le corps est écrit indépendamment contre la spécification MCP 2025-06-18 (`POST` JSON ou SSE, `GET` de flux serveur, `DELETE` de session, en-tête `Mcp-Session-Id`) ; l'implémentation `reqwest` fournie par `rmcp` (MIT, dans le graphe) sert de référence de comportement pour les codes de statut. Écarts assumés : pas d'OAuth par serveur (Codex a `oauth/`, Pyxis lit un jeton porteur par NOM de variable d'environnement) ; pas de re-négociation de scope sur `403` ; URL en clair refusée hors boucle locale, ce que Codex ne fait pas. |
| `crates/agent-mcp/src/config.rs`, `crates/agent-mcp/src/tool.rs` | `codex-rs/config/src/mcp_types.rs` (`enabled_tools` / `disabled_tools`, `default_tools_approval_mode`, table `tools`) | `inspired` | EP-004/US-014 et US-015. Le modèle est repris : allow-list puis deny-list dans cet ordre, et un niveau d'approbation par serveur qu'un réglage par outil surcharge. Le code est écrit indépendamment, en JSON camelCase sur le format `.mcp.json` déjà lu, pas en TOML. Écarts assumés : deux niveaux d'approbation seulement (`ask`, `allow`) parce que le mode de permission, les hooks et la défense taint sont déjà au-dessus et ne peuvent que resserrer ; un réglage d'approbation venant d'un fichier contrôlé par le workspace est écarté avec avertissement (FR-18), ce que Codex ne fait pas ; une entrée qui en masque une autre est réduite à l'intersection des deux filtres au lieu de la remplacer. |
| `crates/agent-tools/src/registry.rs` | `codex-rs/core/src/tools/` (`tool_discovery.rs`, `dynamic_tool.rs`) | `inspired` | EP-004/US-016. L'idée d'un ensemble d'outils qui bouge en session est reprise ; l'implémentation est propre à Pyxis : les changements sont mis en attente et appliqués à la frontière d'un tour (`stage_tools` / `stage_removal` / `commit_staged`), ce qui garantit par construction qu'un tour en cours ne voit jamais son registre changer sous lui. |
| `crates/agent-tools/src/hooks.rs`, `crates/agent-cli/src/main.rs`, `crates/agent-cli/src/interactive.rs` | `codex-rs/hooks/src/lib.rs:20-32` (`HOOK_EVENT_NAMES`), `:35-46` (les événements porteurs de matcher) | `inspired` | EP-005/US-017. Le jeu d'événements est repris sous les mêmes noms (`SessionStart`, `SessionEnd`, `UserPromptSubmit`, `Stop` s'ajoutant à `PreToolUse` et `PostToolUse`), le code est écrit indépendamment. Écarts assumés : 6 événements sur 11, les 5 autres n'ayant aucun point d'accroche dans Pyxis (pas de sous-agent, pas de compaction déclenchée par hook, pas de notification) ; un événement de cycle de vie ne porte PAS de matcher, parce qu'il ne nomme aucun outil, et un matcher déclaré est écarté avec avertissement ; `Stop` et `SessionEnd` observent sans décider, un refus n'y ayant plus rien à arrêter, et surtout un `Stop` bloquant forcerait le modèle à continuer, ce qui serait un hook qui ÉLARGIT le run et non qui le resserre ; `ask` sur un événement de cycle de vie est traité comme un refus, faute de confirmation à demander. |
| `crates/agent-cli/src/skills.rs`, `crates/agent-cli/src/main.rs` | `codex-rs/skills/src/model.rs:8-20` (`SkillMetadata { scope, policy, … }`), `:63-68` (`SkillPolicy { allow_implicit_invocation, … }`), `codex-rs/core-skills/src/root_loader.rs` | `inspired` | EP-005/US-018. Le modèle est repris (une portée par skill, la plus proche l'emportant à nom égal ; une politique qui retire la skill du catalogue implicite sans la rendre inappelable), le code est écrit indépendamment. Écarts assumés : deux portées seulement (`project`, `user`), la racine projet étant CODÉE EN DUR à `<workspace>/.agents/skills` pour qu'un fichier du dépôt ne puisse jamais déplacer la racine ; la politique est une clé plate du frontmatter (`allow-implicit-invocation`, orthographe `snake_case` acceptée) et non une table imbriquée, le lecteur restreint de Pyxis ne lisant pas de YAML imbriqué ; `interface`, `dependencies` et `products` ne sont pas portés, aucune de ces notions n'ayant de consommateur ici ; une skill ne déclare AUCUNE capacité quelle que soit sa portée, ce qui est la condition pour que la portée projet soit ajoutable (FR-18). |
| `crates/agent-parity/**`, `docs/parity/codex-baseline-matrix.json` | `codex-rs/models-manager/models.json`, `codex-rs/protocol/src/openai_models.rs` (`ToolMode`), `codex-rs/protocol/src/protocol.rs` (`MultiAgentVersion`), `codex-rs/core/src/tools/handlers/multi_agents{,_v2}/`, `codex-rs/app-server-protocol/src/protocol/common.rs` | `inspired` | EP-001/US-001 de `tasks/prd-parite-totale-codex-cli.md`. Aucune ligne Codex n'est copiée : le crate lit le clone en lecture seule et en dérive une matrice de faits d'interface (slugs, modes d'outils, versions multi-agent, noms de méthodes app-server). Les noms de valeurs restent ceux de Codex parce que ce sont les identifiants du contrat, pas de l'expression portée. |
| `crates/agent-core/src/provider.rs`, `crates/agent-core/src/message.rs`, `crates/agent-provider/src/chatgpt_request.rs`, `crates/agent-provider/src/chatgpt_events.rs` | `codex-rs/tools/src/responses_api.rs` (`FreeformTool`), `codex-rs/tools/src/tool_spec.rs` (`ToolSpec::Freeform` -> `type: "custom"`), `codex-rs/protocol/src/models.rs` (`CustomToolCall`, `CustomToolCallOutput`), `codex-rs/codex-api/src/sse/responses.rs` (`response.custom_tool_call_input.delta`) | `inspired` | EP-001/US-002 à US-004. Seule la forme du wire Responses est reprise (noms de champs et d'événements imposés par l'API OpenAI, pas par Codex). Les types Pyxis sont provider-neutral (`ToolKind::Freeform`, `ToolCallFormat::Text`) et la projection wire vit exclusivement dans `agent-provider`. |
| `crates/agent-core/src/auxiliary/**`, `crates/agent-core/src/provider.rs`, `crates/agent-provider/src/openai/auxiliary/**`, `crates/agent-provider/tests/conformance/ep006/**` | `/home/arthur/dev/codex/codex-rs/codex-api/src/{common,files,images,search}.rs`, `endpoint/{compact,memories,images,search,realtime_call}.rs`, `endpoint/realtime_websocket/{methods_common,methods_frameless_bidi,methods_v2,protocol,protocol_v1,protocol_v2,protocol_frameless_bidi}.rs` | `adapt` | EP-006 de `tasks/prd-parite-client-modele-codex-api.md`, baseline `ee0247f95a6fe2b094ba2253d82cae2a2b4c2dff`. Les contrats provider-neutral et la surface object-safe vivent dans `agent-core`; seuls les plans de requête, projections wire et transports restent dans `agent-provider`. Les chemins, tags et formes de payload sont des contrats wire reproduits par des types Pyxis écrits indépendamment. Les deux descriptions wire des outils Realtime V2 `background_agent` et `remain_silent` sont conservées verbatim depuis `methods_v2.rs`, car leur texte pilote le comportement du modèle. Tout le code est écrit indépendamment. Écarts assumés : capabilities auxiliaires explicitement opt-in pour un endpoint configuré ; une erreur porte toujours opération et phase sans corps provider ; payloads image/audio et URL signées absents de `Debug` ; réponses inconnues refusées au lieu d'être ignorées ; upload blob construit depuis le client nu sans aucun header provider ; les dialectes Realtime sont des variantes fermées avec codecs séparés ; le client Realtime ne crée aucune tâche et sépare reader/writer sous ownership local, donc close et cancellation n'ont pas d'acteur orphelin. |
| Future app-server runtime integration | `C:\dev\codex\codex-rs\tui\src\history_cell\hook_cell.rs`, app-server hook emitters | `skip` | Pyxis now owns hook transcript types and rendering. Direct app-server hook emission remains out of scope until Pyxis has equivalent runtime events. |
| `crates/agent-code-mode/src/protocol.rs`, `session.rs` | `/home/arthur/dev/codex/codex-rs/code-mode-protocol/src/{session,runtime,response}.rs` | `inspired` | EP-002 US-006. Le contrat cellule (`exec`/`wait`/`terminate`/`shutdown`, `Yielded`/`Terminated`/`Result`, items texte/image/audio) est repris comme CONTRAT ; le code est écrit sur les types Pyxis, avec une machine à états et des erreurs typées propres (`CodeModeError`, `CellFailureKind`). |
| `crates/agent-code-mode/src/tools.rs` | `/home/arthur/dev/codex/codex-rs/core/src/tools/code_mode/{execute_spec,wait_spec}.rs`, `code-mode-protocol/src/description.rs` | `adapt` | EP-002 US-008. La grammaire Lark de `exec` et le préfixe de pragma `// @exec:` sont repris VERBATIM (interopérabilité modèle) ; descriptions et schéma `wait` sont réécrits sur `ToolSpec` Pyxis (schéma strict, `max_output_bytes` au lieu de `max_tokens`). Apache-2.0 : provenance conservée ici et dans `NOTICE-CODEX.md`. |
| `crates/agent-code-mode-v8/src/{lib,engine,globals}.rs` | `/home/arthur/dev/codex/codex-rs/code-mode/src/{v8_init,runtime/mod,runtime/globals}.rs` | `inspired` | EP-002 US-007. Décisions d'architecture reprises : initialisation V8 une fois par processus avec mode JIT figé, un isolate par cellule, store de session hors du global object, retrait de `console`/`Atomics`/`SharedArrayBuffer`/`WebAssembly`. L'implémentation (quotas heap/natif distincts, watchdog CPU, `CellSink`) est écrite pour Pyxis. |
| `spikes/s6-code-mode-v8/**` | `/home/arthur/dev/codex/codex-rs/v8-poc/src/lib.rs` | `inspired` | EP-002 US-005. Le spike mesure l'enveloppe sur le même pin `v8 = "=149.2.0"` et le même ICU ; code de mesure écrit pour ce verdict, jetable. |
| `crates/agent-tools/src/code_mode.rs`, `crates/agent-cli/src/code_mode.rs` | `/home/arthur/dev/codex/codex-rs/core/src/tools/code_mode/**` | `inspired` | EP-002 US-008/US-009. Le découpage outil visible / dispatch imbriqué et le choix de cacher les outils imbriqués en `code_mode_only` viennent de Codex ; le pipeline traversé est celui de Pyxis (`ToolDispatchSnapshot`, permissions, taint, hooks), sans reprise de code. |
| `crates/agent-tools/src/agent.rs` | `/home/arthur/dev/codex/codex-rs/core/src/tools/handlers/multi_agents_spec.rs`, `handlers/multi_agents_v2/**` | `adapt` | EP-003 US-011. Les six NOMS d'outils v2 et leurs noms d'arguments (`task_name`, `message`, `target`, `path_prefix`, `timeout_ms`) sont repris VERBATIM (interopérabilité modèle) ; descriptions, schémas, permissions et comportements sont écrits sur les types Pyxis. Écarts assumés : `wait_agent` accepte un `target` optionnel que la v2 baseline n'a pas (Pyxis attend un enfant précis) et rend un handoff borné plutôt qu'un résumé de boîte aux lettres ; `spawn_agent` expose `tools` (autorité demandée) et n'expose ni `agent_type`, ni `fork_turns`, ni `model`, ni `reasoning_effort`, ni `service_tier` ; aucun outil v1 (`send_input`, `close_agent`, `resume_agent`) n'est porté. Apache-2.0 : provenance conservée ici et dans `NOTICE-CODEX.md`. |
| `crates/agent-runtime/src/path.rs` | `/home/arthur/dev/codex/codex-rs/protocol/src/agent_path.rs` | `inspired` | EP-003 US-011. La forme `/root/<nom>`, la résolution relative depuis la position de l'appelant et les règles de validation d'un segment (minuscules ASCII, chiffres, `_`, `root`/`.`/`..` réservés) sont reprises comme CONTRAT ; le type, ses bornes (64 caractères par segment, 512 par chemin) et ses erreurs typées sont écrits pour Pyxis. `/morpheus` n'est pas porté. |
| `crates/agent-cli/src/subagents.rs` | `/home/arthur/dev/codex/codex-rs/core/src/agent/**` | `inspired` | EP-003 US-011. L'idée d'un enfant qui est un thread complet avec sa propre config est reprise ; le registre d'un enfant est construit depuis son autorité, sans approbateur humain et sans outils multi-agent, ce que Codex ne fait pas. |
| `crates/agent-app-server/**`, `crates/agent-cli/src/app_server.rs`, `docs/app-server/**` | `/home/arthur/dev/codex/codex-rs/app-server-protocol/src/protocol/common.rs` (tables `client_request_definitions!`, `server_request_definitions!`, `server_notification_definitions!`), `protocol/v2/thread.rs` (`dynamic_tools` de `ThreadStart`) | `inspired` | EP-005 de `tasks/prd-parite-totale-codex-cli.md`. Aucune ligne Codex n'est copiée. Sont repris VERBATIM les NOMS de méthodes et de notifications du sous-ensemble P0 (`initialize`, `thread/start`, `thread/resume`, `thread/unsubscribe`, `thread/items/list`, `turn/start`, `turn/steer`, `turn/interrupt`, `thread/started`, `thread/closed`, `turn/started`, `turn/completed`, `item/started`, `item/completed`, `item/agentMessage/delta`, `item/commandExecution/outputDelta`, `serverRequest/resolved`, `error`, `item/commandExecution/requestApproval`, `item/fileChange/requestApproval`, `item/tool/call`), parce que ce sont les identifiants du contrat client, dérivés de la matrice baseline d'US-001. Le décodage, la projection en items, l'ownership, la corrélation des requêtes serveur, la backpressure et les transports sont écrits pour Pyxis. Écarts assumés : 8 méthodes sur les 131 de la baseline (le reste relève des non-goals : comptes, cloud, fs, marketplace, realtime) ; UN thread ouvert à la fois, parce que le registre d'outils, la session Code Mode et le handle multi-agent du processus appartiennent au thread ouvert ; `turn/reasoning/delta` remplace le delta de raisonnement porté par un item, Pyxis ne persistant pas le raisonnement ; `item/tool/requestApproval` ajouté pour les outils qui ne sont ni commande ni changement de fichier ; identifiants d'items `item_<n>` dérivés du rang durable au lieu d'identifiants minté par tour ; transport WebSocket en boucle locale avec jeton porteur, là où Codex n'authentifie pas le sien. |
| `crates/agent-runtime/src/failure.rs`, `crates/agent-cli/src/{failure_line,observability}.rs`, `crates/agent-parity/tests/offline_suite.rs`, `docs/parity/offline-suite.md` | aucune | `inspired` (rien d'adopté) | EP-006 de `tasks/prd-parite-totale-codex-cli.md`. **Aucune ligne, aucun nom et aucune forme ne viennent de Codex.** La taxonomie de causes, la corrélation de traces par spans, la recette offline exécutable et le vérificateur de dérive répondent à des critères propres à Pyxis (une catégorie de cause partagée par quatre surfaces, export distant opt-in par construction, baseline épinglée qui ne suit jamais HEAD) : Codex n'a pas d'équivalent à reprendre. Le clone reste lu, jamais écrit. |
| Connector, realtime, audio, cloud and account surfaces of the app-server | `C:\dev\codex\codex-rs\app-server\**` (méthodes `account/*`, `app/*`, `fs/*`, `marketplace/*`, `thread/realtime/*`, `environment/*`), connector crates | `skip` | Explicit PRD non-goals. |
| `crates/agent-core/src/{event,permission,provider,quota,message,tools}.rs`, `crates/agent-provider/src/{chatgpt_events,chatgpt_request,quota}.rs`, `crates/agent-tools/src/{registry,tool,bash,exec_session,patch,turn_diff,permission}.rs`, `crates/agent-mcp/src/tool.rs` | `codex-rs/protocol/src/models.rs`, `codex-rs/protocol/src/protocol.rs`, `codex-rs/codex-api/src/{rate_limits.rs,sse/responses.rs}` | `inspired` | Audit de parité du 2026-07-31 : dix faits que la référence porte et qui n'atteignaient aucun client ici. Les NOMS de fil sont repris parce qu'ils sont le contrat (`input_text`/`input_image` dans `function_call_output.output`, `cached_tokens`/`cache_write_tokens`/`reasoning_tokens`, `x-codex-credits-*`, `x-codex-rate-limit-reached-type`, `codex.rate_limits`) ; le code est écrit indépendamment. Écarts assumés qui subsistent : un seul `ToolCall` QUALIFIÉ (`ToolCallKind`) au lieu des trois familles `ExecCommandBegin/End`, `PatchApplyBegin/End` et `McpToolCallBegin/End` ; `HookRunStatus` réduit à trois valeurs, Pyxis n'ayant ni run observable en cours ni arrêt de session par hook ; `InterruptReason` à deux valeurs, l'épuisement de budget voyageant déjà en `Exhausted` et un steer ne coupant pas le tour ; `PermissionMode` reste le modèle Pyxis à cinq modes, pas `AskForApproval`. |

## Divergences de rendu assumées (US-006)

Les snapshots de `crates/agent-tui/tests/snapshots/` figent le rendu réel de
Pyxis. Les écarts ci-dessous par rapport au rendu Codex sont **intentionnels** :
un diff de snapshot qui les fait disparaître doit être traité comme une
régression, pas comme un rapprochement de parité.

| Divergence | Snapshot témoin | Raison |
|---|---|---|
| Composer ancré en bas du terminal, encadré par deux filets pleine largeur, transcript rendu au-dessus | `welcome`, `pending_input` | Choix de mise en page Pyxis déjà enregistré pour `render.rs` dans l'inventaire ci-dessus. Codex encadre son composer de lignes vides ; Pyxis garde les filets, qui lisent la saisie comme un champ borné, et pose la status line directement sous le filet de fermeture (forme Claude Code). |
| Menu de complétion ouvert AU-DESSUS du composer | `command_menu`, `command_menu_filtered`, `resume_menu` | Codex rend ses popups en dessous, à la place du pied de page. Pyxis garde l'ouverture vers le haut : le regard reste sur la ligne de saisie, qui ne bouge pas quand la liste grandit. Divergence assumée et demandée. |
| Écran d'accueil : carte bordée avec logo pixel et raccourcis, au lieu de l'accueil textuel Codex | `welcome`, `welcome_narrow`, `welcome_wide` | Signature visuelle Pyxis (monochrome + un accent). |
| Pied de page à la grammaire Codex : status line `modèle effort · workspace` à gauche, mode de permission à droite hors valeur par défaut | tous les snapshots, `footer_status_line`, `footer_status_line_narrow` | Adopté le 2026-07-31. La jauge de contexte `▰▱` qui occupait cette ligne a été retirée : comme chez Codex, la consommation se lit dans `/status` et `/usage`, pas dans le pied de page. |
| Pense-bête des raccourcis ouvert par `?` sur composer vide | `footer_shortcut_overlay` | Porté de l'overlay Codex, restreint aux touches que Pyxis lie réellement. Pyxis n'a pas de `/keymap` : le pied de la liste dit comment la fermer au lieu de comment la personnaliser. |
| Résumé d'outil sur un connecteur `⎿` et diff inline dérivé de l'input du call | `exec_success`, `exec_error`, `edit_diff` | Pyxis dérive le diff de l'appel d'outil, sans cellule d'exécution dédiée. |
| Dialogue d'approbation rendu en bas de frame sans overlay modal | `approval_dialog` | Le dialogue remplace la zone de saisie plutôt que de recouvrir le transcript. |
| Composer multi-ligne : gouttière `› ` sur la première ligne, alignement à deux colonnes sur les continuations, plafond de 10 lignes de texte puis défilement | `composer_multiline`, `composer_scrolled`, `composer_wrapped_line` | Repli et défilement calculés par `crates/agent-tui/src/composer.rs` : les lignes visuelles partitionnent l'input, ce qui rend la position du curseur exacte. Codex passe par sa propre pile de vues. |
| Un token `/skill` ou `@fichier` coupé par un repli perd sa surbrillance | `composer_wrapped_line` | La surbrillance est calculée par segment visuel. Restituer un span à cheval sur deux lignes coûterait un découpage de spans pour un gain cosmétique nul. |
| Navigation verticale par ligne LOGIQUE, pas par ligne visuelle | `composer_wrapped_line` | Haut/Bas sautent le paragraphe replié entier. Le modèle de saisie reste ainsi indépendant de la largeur de rendu. |
| `ctrl + j for newline` annoncé dans le pense-bête `?` et non dans le pied de page | `footer_shortcut_overlay` | Ctrl+J est le seul raccourci d'insertion indépendant du protocole clavier du terminal (US-009 AC3), donc il est annoncé ; depuis le 2026-07-31 il l'est dans le pense-bête, la ligne de pied étant rendue à la status line comme chez Codex. |

EP-003 de `tasks/prd-harness-parity.md` a levé la limitation qui figurait ici :
`resize_narrow` montrait une saisie tronquée à la largeur du terminal, elle est
maintenant repliée sur deux lignes visuelles. Le diff de ce snapshot est la
preuve du correctif.

## Refonte de la surface de chat (2026-07-31)

Le chemin `codex_tui_parity` rend désormais le chat comme Codex : l'historique
finalisé appartient au scrollback du terminal, le viewport ne porte que ce qui
est encore mutable. Les entrées ci-dessous complètent l'inventaire et la notice
`NOTICE-CODEX.md`.

| Fichier Pyxis | Source | Nature |
|---|---|---|
| `crates/agent-tui/src/custom_terminal.rs` | `ratatui::Terminal` / `ratatui::Frame` (0.29.0, **MIT**) | Dérivé, licence conservée en tête de fichier. Rend `viewport_area` modifiable, ce que ratatui n'expose pas. |
| `crates/agent-tui/src/insert_history.rs` | `ratatui::Terminal::insert_before` (chemin `scrolling-regions`, **MIT**) | Stratégie de découpe des lignes adaptée. |
| `crates/agent-tui/src/parse_command.rs` | `codex-rs/shell-command/src/parse_command.rs`, `codex-rs/protocol/src/parse_command.rs` | Structurellement dérivé : classification Read/ListFiles/Search/Unknown. |
| `crates/agent-tui/src/streaming.rs` (politique de cadence) | `codex-rs/tui/src/streaming/{chunking,commit_tick}.rs` | Structurellement dérivé : modèle Smooth/CatchUp à hystérésis. |
| `ExecCell` (groupement `Explored`) dans `history_cell.rs` | `codex-rs/tui/src/exec_cell/{model,render}.rs` | Structurellement dérivé. |
| `term::draw` / `ChatSurface::reflow` | `codex-rs/tui/src/tui.rs`, `codex-rs/tui/src/app/resize_reflow.rs` | Structurellement dérivé : ancrage du viewport, reflow débouncé. |

### Divergences supplémentaires assumées

| Divergence | Snapshot témoin | Raison |
|---|---|---|
| Un appel exec en échec quitte le groupe `Explored` et retrouve son rendu complet | `parity_surface_exploring_then_streaming` | Le groupe dit ce qui a été consulté, pas ce qui est revenu. Y replier un échec masquerait le message d'erreur. Codex garde l'appel dans le groupe. |
| Coloration syntaxique active pendant le streaming | `parity_surface_exploring_then_streaming` | Une ligne partie dans le scrollback ne peut plus être repeinte : rendre la première moitié d'un message sans coloration et la seconde avec serait définitif. |
| Composer ancré sur la dernière ligne de l'écran, quelle que soit la longueur du fil | `parity_surface_conversation`, `parity_surface_exploring_then_streaming` | Divergence Pyxis conservée à travers la refonte du viewport. Codex laisse son composer suivre la fin de l'historique, donc flotter au milieu de l'écran tant que la session est courte. Pyxis garde le viewport ancré en bas : il s'étend de la fin de l'historique jusqu'à la dernière ligne, le vide se place entre la cellule active et le composer, et la ligne de saisie ne bouge jamais. L'historique reprend ses rangées par le haut du viewport. |
| Écran d'accueil rendu dans le viewport, jamais inséré dans le scrollback | `parity_surface_conversation` | Il disparaît au premier message au lieu de rester en haut de l'historique comme l'en-tête de session Codex. La carte Pyxis est une signature de démarrage, pas un enregistrement de session. |
| Une ligne repliée par le terminal perd ses hyperliens (le texte reste intact) | `parity_surface_conversation` | Les colonnes d'un lien décrivent une rangée. Une ligne source qui occupe plusieurs rangées ne permet plus de dire laquelle porte le lien ; marquer au hasard vaut moins que ne pas marquer. Les cellules pré-replient leur rendu, donc le cas est résiduel. |
| Le reflow au resize ne réécrit que les rangées encore visibles que Pyxis a écrites | `parity_surface_conversation` | Codex purge tout le scrollback (`ESC[3J`), ce qui efface aussi ce que l'utilisateur avait dans son terminal avant la session. Pyxis ne reprend que ses propres rangées visibles ; ce qui a défilé garde son ancien repli, comme la sortie de n'importe quel programme. |
